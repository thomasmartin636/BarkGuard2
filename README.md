# Bark Guard

Listens for barking and plays a high-frequency interrupt tone. Free for 1 hour a day, with an unlimited subscription through Stripe.

Hosted on Cloudflare Pages: `public/` is the site and `functions/api/` is the Stripe backend. See **SETUP.md** for deployment steps.
